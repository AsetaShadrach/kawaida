{
  "name": "kawaida",
  "version": "1.0.0",
  "description": "Contains all modules, entities, constants, shared across the microservices",
  "author": "AsetaShadrach",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "class-validator": "^0.14.0",
    "rimraf": "^5.0.1",
    "rxjs": "^7.8.1",
    "typescript": "^5.1.6"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "yarn run build"
  }
}
